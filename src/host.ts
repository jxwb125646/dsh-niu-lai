/**
 * dsh-niu-lai sound board plugin, node half.
 *
 * Deliberately empty: everything this plugin does — watching session
 * snapshots, playing the meme clips, and the settings tab — lives in the
 * browser half. The Host has no sound to play and no data to compute.
 */

/** Host plugin body — nothing to mount server-side. */
export function apply(): void {}
